'use strict';

/**
 * subscription-button router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscription-button.subscription-button');
